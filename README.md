

Quick script to drop in PATH and easily generate placeholders from OpenRouter.
Just add a model and a key

## Usage

Usage: python generate_image.py '<prompt>' [-i <image_path>]
Example:

```bash
generate_image.py 'A beautiful sunset over mountains'
generate_image.py 'remove the background' -i my_img.png
```
